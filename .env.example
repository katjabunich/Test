NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Comma-separated invite codes required for signup. Server-only — never
# exposed to the client. Rotate by editing this in Vercel; redeploy is
# automatic. At least one non-empty code must be set or signup is closed.
INVITE_CODES=
