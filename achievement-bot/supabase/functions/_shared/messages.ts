// Тексты бота. Тёплый тон, акцент на ТВОЮ роль (а не «что просто случилось») —
// это ключ против обесценивания (см. исследования по self-esteem journaling).

export const WELCOME = `Привет 🤗

Я твой личный дневник достижений. Сюда ты записываешь хорошее, что случилось — *в том числе и благодаря тебе*. Даже маленькое. Даже то, что кажется «само получилось».

Как пользоваться — проще некуда:
• Просто *напиши мне* что произошло. Я сохраню.
• Каждый вечер я мягко спрошу тебя сам, чтобы ты не забывала.
• Раз в неделю пришлю список — посмотреть, сколько всего ты сделала.

Команды (по желанию):
/week — что было за неделю
/итоги — разбор: какие у тебя повторяются сильные стороны
/export — выгрузить все записи
/time 21:00 — поменять время вечернего вопроса
/pause — поставить напоминания на паузу /resume — вернуть
/help — это меню

Попробуй прямо сейчас: напиши одно хорошее, что было сегодня ✨`;

export const HELP = `Что я умею:

• Просто напиши мне — сохраню как достижение.
/week — список за последние 7 дней
/итоги — AI-разбор твоих сильных сторон и паттернов
/export — выгрузить всё текстом
/time 21:00 — время вечернего вопроса
/pause — пауза напоминаний /resume — включить обратно
/help — это меню`;

export const PRIVATE_BOT = 'Это личный бот 🙅 Он работает только со своим владельцем.';

// Подтверждения записи — лёгкая ротация, чтобы не приедалось.
export const SAVED = [
  'Записала ✨',
  'Сохранила 🌱',
  'Есть, записала 💛',
  'Зафиксировала ✨',
  'Поймала момент 📌',
];

// Кнопка-уточнение про твою роль (появляется не каждый раз).
export const AGENCY_PROMPT = 'А в чём тут была *твоя* заслуга — даже маленькая?';
export const AGENCY_BTN = '✍️ Допишу';
export const AGENCY_SKIP_BTN = 'Пропустить';
export const AGENCY_WAIT = 'Слушаю — в чём твой вклад? (или /skip)';
export const AGENCY_SAVED = 'Вот это важно записать 💪 Добавила.';
export const AGENCY_SKIPPED = 'Ок, и так хорошо ✨';

// Вечерний вопрос — с акцентом на роль.
export const EVENING_PROMPTS = [
  'Что хорошего случилось сегодня — и какая в этом *твоя* заслуга, даже маленькая?',
  'Чем ты сегодня можешь гордиться? Даже мелочь считается.',
  'Что сегодня получилось *благодаря тебе*?',
  'Какой свой шаг сегодня — пусть крошечный — ты хочешь отметить?',
];

export const PAUSED = 'Поставила напоминания на паузу. Включить обратно — /resume 🌙';
export const RESUMED = 'Готово, снова буду напоминать по вечерам ✨';
export const TIME_SET = (h: number) =>
  `Хорошо, буду спрашивать в ${String(h).padStart(2, '0')}:00 🌙`;
export const TIME_BAD = 'Не разобрала время. Формат: /time 21:00';

export const EMPTY_WEEK =
  'За последнюю неделю записей пока нет. Это не «ничего не было» — просто ещё не пойманное. Напиши хоть одно 💛';
export const EMPTY_EXPORT = 'Записей пока нет — как появятся, выгружу.';

export const weekHeader = (n: number) =>
  `Вот что ты сделала за неделю — ${n} ${plural(n, 'запись', 'записи', 'записей')} 💛\n`;

export const NO_AI_KEY =
  'AI-разбор пока не подключён (нужен ключ Claude API). Запись и сводки работают и без него. ' +
  'А пока можешь сделать /export и закинуть текст в обычный Claude по подписке.';

export const AI_THINKING = 'Читаю твои записи и ищу закономерности… 🔎';
export const AI_TOO_FEW =
  'Пока маловато записей для разбора — добавь ещё несколько, и я найду в них паттерны ✨';

export function plural(n: number, one: string, few: string, many: string): string {
  const m10 = n % 10;
  const m100 = n % 100;
  if (m10 === 1 && m100 !== 11) return one;
  if (m10 >= 2 && m10 <= 4 && (m100 < 10 || m100 >= 20)) return few;
  return many;
}

export function pick<T>(arr: T[]): T {
  return arr[Math.floor(Math.random() * arr.length)];
}
