# Edge Functions вызываются Телеграмом и pg_cron без JWT — отключаем проверку,
# защита у нас своя (секрет webhook'а и секрет cron'а в заголовках).
[functions.achv-bot]
verify_jwt = false

[functions.achv-cron]
verify_jwt = false
